import React from 'react';

const Loading = () => {
    return (
        <div class="flex justify-center items-center h-screen">
            <p>Processing...</p>
        </div>
    );
};

export default Loading;